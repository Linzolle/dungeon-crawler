using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class PlayerList
{
    public static Entity player = new Entity("Player", 100, 5, 5, 5, 5, new List<Element>(), new List<Element>());
}
